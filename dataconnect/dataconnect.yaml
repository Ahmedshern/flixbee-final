specVersion: "v1beta"
serviceId: "Buzzplay"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Buzzplay-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
